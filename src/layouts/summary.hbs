<div class="section">
    <a href="{{article.url}}" class="title">
        <h1>
            {{article.data.title}}
        </h1>
    </a>
    <p>
        <i>
            {{ dateFormat article.date }}
        </i>
    </p>
    <p>
        {{article.data.summary}}
    </p>
</div>